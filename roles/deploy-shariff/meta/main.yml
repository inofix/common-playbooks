dependencies:
  - copy
  - { role: set-credentials, file_list: "{{ account_file_list }}", file_type: "{{ account_file_type }}", account_section: 'shariff_backend_id', account_key: 'app_id' }
  - { role: set-credentials, file_list: "{{ account_file_list }}", file_type: "{{ account_file_type }}", account_section: 'shariff_backend_secret', account_key: 'secret' }
# ommit comment in json files
#  - mark-managed
